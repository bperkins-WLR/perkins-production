{
    "name": "perkins-production",
    "version": "1.0.0",
    "private": true,
    "type": "module",
    "engines": {
        "node": ">=20.9.0"
    },
    "scripts": {
        "check": "node --check api/*.js lib/*.js && bash -n scripts/health-check.sh",
        "test": "node --test"
    },
    "dependencies": {
        "@vercel/blob": "^2.8.0",
        "sharp": "^0.35.3"
    }
}
